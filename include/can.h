#ifndef CAN_H
#define CAN_H

#include <channel.h>


class can : public channel
{
        public:
                can();
                virtual ~can();
        protected:
        private:
};

#endif // CAN_H
